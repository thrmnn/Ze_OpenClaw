# VPS Audit — 2026-03-24

**Audited by:** Zé (subagent)  
**Date:** Tuesday, March 24, 2026 · 17:00–17:15 UTC  
**Host:** ubuntu-8gb-hel1-1  

---

## 1. OpenClaw Health

### Doctor / Status
- ✅ Gateway: running (systemd, pid 93543, active)
- ✅ Telegram channel: OK (`@Tzinho_lclclawdbot`)
- ✅ Plugins: 8 loaded, 0 errors (33 disabled — normal)
- ✅ Skills: 13 eligible, 42 missing requirements (expected — optional integrations)
- ✅ Session store: healthy, 8 entries, last activity just now
- ✅ Memory: 38 files, 38 chunks, vector ready
- ⚠️ **Update available:** `2026.3.23-2` (current: `2026.3.13`) — run `openclaw update`
- ⚠️ **Gateway reachability:** `unreachable (missing scope: operator.read)` — local loopback only, no remote access configured (Tailscale reported as "off" in status despite tailscaled running)
- ⚠️ **Security warnings (3):**
  - Reverse proxy headers not trusted (low risk, no proxy in use)
  - `denyCommands` uses exact name matching only (minor config issue)
  - `claude-haiku-4-5` used in some agent — below recommended tier

### Session Store
- ✅ 8 active sessions (3 live: main, Telegram, this subagent)
- ✅ Duplicate cron session entries for `1dd0ba41` (Position Radar) — stale, not alarming

---

## 2. Syncthing

- ✅ **Service running:** active since Mar 23 00:17 UTC (1 day 16h uptime)
- ✅ **All 4 vaults configured and syncing:**
  - `Ob_Business_Vault`: 126 files, state idle, 0 errors, 0 needFiles
  - `Ob_Perso_Vault`: 60 files, state idle, 0 errors, 0 needFiles
  - `Ob_Research_Vault`: 42 files, state idle, 0 errors, 0 needFiles
  - `Ob_Robotics_Vault`: 44 files, state idle, 0 errors, 0 needFiles
- ⚠️ **Peer currently disconnected** (JK36Z5W / "MSI" laptop) — last connected at 14:47 UTC, disconnected at 15:17 UTC (read timeout). Normal intermittent behaviour but worth monitoring.
- ⚠️ **1 sync conflict file found:**
  - `Ob_Perso_Vault/Daily/2026-03-24.sync-conflict-20260324-120401-JK36Z5W.md`
  - Likely created when both devices modified today's daily note simultaneously. Not critical, but should be reviewed and resolved.
- ✅ No errors or irrecoverable states

---

## 3. Services & Processes

### Memory
- ✅ Total: 7.6 GiB | Used: 2.4 GiB | Free: 3.5 GiB | Available: 5.2 GiB
- ✅ Buff/cache: 2.0 GiB (healthy)
- 🔴 **No swap configured** — if openclaw-gateway has a memory spike, OOM kill risk is real

### Disk
- ✅ Root (`/dev/sda1`): 75G total, 12G used (16% — healthy)
- ✅ EFI: 253M, 1% used
- ✅ No disk pressure

### CPU/RAM Top Processes
| Process | %MEM | Notes |
|---------|------|-------|
| `openclaw-gateway` | 22.6% (1.8 GiB RSS) | Main gateway process — elevated but expected |
| `syncthing` | 0.6% (50 MB) | Normal |
| `tailscaled` | 0.5% (47 MB) | Normal |
| `fail2ban` | 0.4% (36 MB) | Normal |
| `qdrant` | 0.4% (36 MB) | Normal |
| `dockerd` | 0.5% (40 MB) | Running but likely idle |

- ⚠️ **openclaw-gateway at 22.6% RAM (1.8 GiB)** — has been running since Mar 24 02:43 UTC, accumulated 36min CPU. Not runaway, but worth monitoring for memory leaks over time.
- ⚠️ **Docker running** but no active containers (only containerd-shim for 1 stopped container). Consider removing unused containers/images to reclaim disk space.

---

## 4. Cron Audit

**Total jobs: 32**

### 🔴 Issues

| ID | Name | Issue |
|----|------|-------|
| `1dd0ba41` | **Position Radar Daily Scan** | `error` state. Root cause: runs `isolated` (no API key inheritance). Last error: `FailoverError: No API key found for provider "anthropic"`. Cron itself works (ran successfully on 2026-03-20) but isolated context breaks auth. Needs fix. |

### ⚠️ Duplicates

| IDs | Names | Issue |
|-----|-------|-------|
| `432bde18` + `4760c43b` | **Friday Weekly Review + Content Draft** / **Weekly Review (Friday)** | Both scheduled `0 18 * * 5` (Fri 15:00 Brazil). **Exact duplicate time.** One should be removed or they'll both fire. |

### ⚠️ Potential Overlap / Redundancy

| ID | Name | Notes |
|----|------|-------|
| `ba3ed420` | **Health Admin - This Sunday** | One-time `at` for 2026-03-29 23:00Z. Likely intentional, but duplicates the recurring `d0d01d9f` Monthly Health Admin which also fires Sundays. Review after it runs. |
| `ed359955` | **Morning Health Brief** | Fires 07:30 Mon-Fri. Overlaps with `5a87f0af` Health Morning Log at 08:00. Both ask for health check-in. Could consolidate. |

### ✅ OK Jobs (summary)

| Schedule | Jobs |
|----------|------|
| Every 2m | mc-sync-agents |
| Daily (morning) | Morning Brief v2, daily-qmd-update, Morning Health Brief, Health Morning Log, Obsidian Morning Routine, Daily Note Generator, Trading Pre-Market Scan, Position Radar (broken) |
| Daily (evening) | End-of-Day Review, Persist Gateway Logs, Daily Auto-Commit |
| Weekdays | Pre-Workout Meal Reminder |
| Weekly (Friday) | Weekly Review (×2 = duplicate), Friday Clean |
| Weekly (Sunday) | Weekly Progress Check, Obsidian Git Backup, Monthly Health Admin, Weekly Digest Email, Sunday Mini-Review, Sunday Grooming |
| Weekly (Monday) | Weekly Kickoff |
| One-time | Gym Reminder Today, Health Admin This Sunday, Anniversary Prep |
| Monthly | Anniversary Reminder |

---

## 5. Scripts Audit

### calendar-check.sh
- ✅ **Working** — returned "No events in the next 24 hours" cleanly

### morning-brief-v2.py --dry-run
- ✅ **Working** — all 5 modules ran successfully:
  - ✅ jobs: OK (13 active, 13 to submit)
  - ✅ tasks: OK
  - ✅ calendar: OK
  - ✅ email: OK
  - ✅ intel: OK (5 arXiv papers fetched)
- ✅ Brief generated and saved to `memory/briefs/2026-03-24.md`

### fetch_jobs.py --test
- ✅ **Working** — returned JSON with 13 active, 13 to_apply
- ⚠️ `radar_note: "last_results.json not found"` — Position Radar has never completed successfully this week (confirmed: in error state since auth broke)

---

## 6. Obsidian Vaults

- ✅ All 4 vaults accessible at `~/obsidian-vaults/`
- ✅ File counts (Markdown only):
  | Vault | .md files |
  |-------|-----------|
  | Ob_Business_Vault | 68 |
  | Ob_Perso_Vault | 55 |
  | Ob_Research_Vault | 34 |
  | Ob_Robotics_Vault | 39 |
- ⚠️ **1 sync conflict:** `Ob_Perso_Vault/Daily/2026-03-24.sync-conflict-20260324-120401-JK36Z5W.md` — needs manual resolution (compare with original `2026-03-24.md` and merge or delete conflict copy)

---

## 7. Projects Health

### job-pipeline (`~/clawd/job-pipeline/`)
- ✅ Directory exists with full structure (main.py, orchestrator.py, requirements.txt, tracker.db, etc.)
- ⚠️ **No dedicated venv** — pipeline relies on system Python or `venv-google`, but `venv-google` is missing `beautifulsoup4`, `selenium`, and other scraping deps
- 🔴 **Not runnable as-is from system Python** — `pydantic` missing from system; `bs4` missing from venv-google
- ✅ Core logic imports work if sourcing `venv-google` + only using orchestrator-level imports
- ⚠️ `last_results.json` not found — Position Radar pipeline has no recent successful run output

### mission-control (`~/clawd/mission-control/`)
- ✅ **This is the real one** — full Next.js app with Convex backend, deployed to Vercel
- 🔴 `~/projects/mission-control/` — **does not exist** (empty `~/projects/` directory)
- ✅ Mission Control is live at `https://mission-control-ruby-zeta.vercel.app/`
- ⚠️ `~/projects/` directory is empty — leftover placeholder, can be removed or repurposed

---

## 📋 Improvement Plan (Prioritized)

### 🔴 P1 — Fix Now

1. **Fix Position Radar auth (isolated context)**
   - Root cause: cron runs in `isolated` mode without inheriting the main agent's API key
   - Fix option A: Change target from `isolated` to `main` (simplest)
   - Fix option B: Configure auth for the isolated agent context
   - Command: `openclaw cron edit --id 1dd0ba41-971c-40b6-b082-966c04d2ed1a --target main`

2. **Remove duplicate Weekly Review cron**
   - `432bde18` (Friday Weekly Review + Content Draft) and `4760c43b` (Weekly Review (Friday)) fire at the exact same time every Friday
   - Decide which one to keep (likely the more specific `432bde18`), then: `openclaw cron rm 4760c43b-189a-466c-9e67-dc439d8370a3`

3. **Resolve Obsidian sync conflict**
   - Compare `Ob_Perso_Vault/Daily/2026-03-24.md` vs `.sync-conflict-...JK36Z5W.md`
   - Merge any unique content, then delete the conflict file

### ⚠️ P2 — Address Soon

4. **Update OpenClaw** (`2026.3.13` → `2026.3.23-2`)
   - Run: `openclaw update`

5. **Add swap space**
   - 0 swap is a risk on an 8GB VPS running multiple services + gateway at 1.8GB RAM
   - Add 2-4GB swap file: `sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile`
   - Make persistent: add to `/etc/fstab`

6. **Create dedicated venv for job-pipeline**
   - `cd ~/clawd/job-pipeline && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
   - Update any scripts that invoke pipeline to use `.venv`

7. **Review morning health crons overlap**
   - `Morning Health Brief` (07:30) vs `Health Morning Log` (08:00) — both ask for health check-in
   - Consider consolidating into one cron

### 📝 P3 — Low Priority / Nice-to-Have

8. **Clean up `~/projects/` directory** — empty placeholder, repurpose or remove

9. **Review `Health Admin - This Sunday` after it runs** (2026-03-29) — if the monthly recurring cron covers it, delete the one-time version

10. **Monitor gateway memory** — 1.8GB RSS is elevated; restart gateway if it continues growing (`openclaw gateway restart`)

11. **Prune Docker** — `docker system prune` to reclaim any space from stopped containers/unused images

12. **Address gateway reachability** — configure Tailscale integration in openclaw if remote gateway access is desired

---

## Summary

| Category | Status |
|----------|--------|
| OpenClaw Gateway | ✅ Running, ⚠️ update pending |
| Telegram Channel | ✅ OK |
| Crons | 🔴 1 broken (Position Radar), ⚠️ 1 duplicate (Weekly Review) |
| Syncthing | ✅ Running, ⚠️ peer offline, ⚠️ 1 conflict |
| Memory | ✅ Healthy (5.2GB free), 🔴 No swap |
| Disk | ✅ 16% used |
| Scripts | ✅ All 3 key scripts working |
| Vaults | ✅ All 4 accessible, ⚠️ 1 conflict |
| Job Pipeline | ⚠️ No venv, partially runnable |
| Mission Control | ✅ Live (clawd/mission-control) |
